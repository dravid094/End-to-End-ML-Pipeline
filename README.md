# End-to-End-ML-Pipeline

# Step_1- Create a conda environment after opening the repository
conda create -n mlops python=3.11 -y 

conda activate mlops

# Step_2- Install the requirements
pip install -r requirements.txt